import { ConvertToBrPipe } from './convert-to-br.pipe';

describe('ConvertToBrPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToBrPipe();
    expect(pipe).toBeTruthy();
  });
});
