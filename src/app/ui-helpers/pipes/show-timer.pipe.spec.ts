import { ShowTimerPipe } from './show-timer.pipe';

describe('ShowTimerPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowTimerPipe();
    expect(pipe).toBeTruthy();
  });
});
